export function ClassificationBanner() {
  return (
    <div
      className="w-full text-center py-1.5 font-mono text-[11px] font-semibold tracking-[0.18em] uppercase"
      style={{ background: '#0e6b41', color: '#daffe9' }}
    >
      Unclassified // Prototype — For Demonstration Only
    </div>
  );
}
